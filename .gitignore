# macOS Finder metadata
.DS_Store

# local working clone of sepahead/melkor, not part of the profile repo
/melkor/

# Installed Node dependencies are local build artifacts. Package manifests are
# intentionally not ignored so future tooling changes cannot be hidden.
node_modules/

# scratch render/screenshot artifacts from local SVG previews
/preview.html
/cxzoom.html

# local AI-tool session/config dirs, machine-specific
/.claude/
/.devin/
/.playwright-mcp/
/.qoder/
